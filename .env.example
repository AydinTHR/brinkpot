# Copy this file to .env and fill in real values. Never commit the real .env.
# .env is already listed in .gitignore.

# Example variables. Replace with what the app actually needs.
APP_ENV=development
LOG_LEVEL=info
# DATABASE_URL=
# SECRET_KEY=
